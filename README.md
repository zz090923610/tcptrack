### tcktrack

Forked version, with support to show high speeds > 100MB/s.

### build

```shell

# dependencies:

sudo apt install libncurses-dev libpcap-dev 

# make, install

./configure && make -j
make install

```
